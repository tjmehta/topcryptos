# Earlier-era Radar replication

Frozen September 14, 2026 UTC before this replication's outcomes. Reuse the parent Radar protocol and implementation, restricted to daily view 7, boundaries 300→200 and 200→150, and holds 7/14/30 saved daily steps. Preserve every primary entry, fee, missing-value, ten-slot, candidate selection, chronology and path-label convention. No new score parameters or automatic recommendation are selected here.

The source is the 110-day November 18, 2020–March 7, 2021 local CMC cache block inventoried in `research/coin-outlook/2026-09-14/FINANCIAL_FOUNDATION_inventory.json`. Use those exact selected filenames and hashes, not a current-survivor list. Normalize each snapshot's `time` and `decision` to the latest valid historical quote timestamp. This is **effective quote-time replay of retrospective historical reconstructions**: status timestamps remain separately recorded, and 104/110 were returned more than one day later. We do not claim original information vintage, actual signal availability then, or an untouched later holdout. No upstream acquisition or gap filling.

Primary comparisons: rank-confirmed fresh crossers sorted by fractional rank velocity; the identical candidates sorted by price momentum; all fresh crossers by numeric ID; positive-price-momentum assets in the same half-boundary-to-boundary rank band. Ten slots, unused cash, next-snapshot entry and fixed-horizon exit; costs 0.5% and 1% each side, primary missing exits at total loss plus zero-gross sensitivity. These are overlapping basket returns, not compounded wealth or US-exchange fills.

Keep milestone touch and endpoint membership separate from price returns. For NEW milestone interpretation restrict the reported denominator to positions above the next milestone at signal; a large crossing that already reached it must not count as a successful future arrival. Record missing paths and sampled +20%, +100% and +400% price touches from entry independently. An observed 5× touch is not a realized 5× sale.

Exploratory incremental-sort diagnostic, predeclared here: select the first three confirmed crossers from each of the frozen rank-velocity and price-momentum orderings. Use three fixed slots with unused cash. Report all matched signal dates, and separately dates with more than three confirmed crossers, where truncation can change membership. Report count of differing selections, unique coins and paired net differences at both fees/missing marks. Do not choose thresholds or claim ranking superiority from ties or a handful of contested dates. This top-three experiment is separate from the primary ten-slot protocol.

Preserve parent runner/protocol/verifier hashes, inventory hash, normalized source and raw source hashes. Reuse the independently written parent verifier with source-path adjustments; verify all selected IDs, timestamps, prices, Decimal fees and phase arithmetic. Independently check the top-three selected membership and Decimal basket arithmetic. Retain all primary cells and negative results. Earlier/modern directional agreement is robustness evidence conditional on the two samples, never a calibrated live coin probability.
