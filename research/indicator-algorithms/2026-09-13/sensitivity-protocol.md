# Initial-date sensitivity — frozen 2026-09-13 UTC

The primary indicator race and annual selector have already been evaluated. Several fixed breakout/H30 policies and the view4 annual selector have favorable terminal results, raising an unresolved concern about initial calendar phase. This is a post-result descriptive robustness check, not new training, a holdout, or a rule for choosing a favorable phase.

Use the immutable main cohort ledger, existing annual choices, and existing selector replay. Preserve all six methods × five short holdings, AnnualSelector, and Cash for all twelve views. Start with unit capital available on January 1, January 29, or February 26 of 2023 (offsets0/28/56 days). Retain the original signal calendar and common final cash-marking boundary March1,2026. Exclude only signals preceding the specified initial available date; afterward enforce signal>=previous hard exit, including intervals with empty selections. Annual choices remain exactly those already frozen in the primary run. Do not refit or tune any method, holding, selector, threshold, or phase.

For each path retain terminal wealth, exit-checkpoint drawdown, executed signal IDs, and the same four missing-price/cost scenarios. Also report a concentration diagnostic replacing the best executed cohort's return multiplier with1 while keeping every entry, exit and later scheduled signal unchanged. Determine that cohort separately per scenario using its largest arithmetic cohort return; ties use earliest execution. If no cohorts execute, report unchanged cash wealth and no omitted signal. This diagnostic is retrospective and can increase wealth if every executed cohort lost money; label the selected cohort return explicitly. Do not divide terminal wealth by its multiplier, because a total-loss multiplier may be zero.

Verify all phase0 results exactly reproduce the primary384 summaries, all phases respect initial capital availability and nonoverlap, and every cohort retains denominator10 even if some slots are empty. Preserve hashes and refuse output overwrite. The three phases share most source prices; they are not independent samples or statistical confidence intervals.
