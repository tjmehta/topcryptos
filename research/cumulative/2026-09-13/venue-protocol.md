# Within-window cumulative venue test — 2026-09-13 UTC

Written before evaluating the new candidates. All underlying price data and previous strategy results have already been inspected. This is retrospective evaluation, not an untouched holdout or proof about the current CMC default.

Precision amendment, before any result artifacts: the initial run was interrupted during formation scoring after independent synthetic review found cancellation dust. Preserve its runner/protocol in `venue-initial/`. Set the summed trapezoid area to exactly zero when its absolute value is at most `32 * 2^-52 * sum(abs(trapezoid contributions))`, before dividing by elapsed duration. This matches the application's precision guard and prevents economically zero paths from receiving a signed percentile. Synthetic paths `[100,200,100,50,100]` and `[1,2,1,0.5,1]` must both give zero. No economic parameter or strategy rule changed.

Use every frozen daily formation from `research/daily-breakouts/2026-09-13`, with unchanged annual identities, 60-complete-bar/lagged-liquidity eligibility and all twelve complete viewed windows. Preserve the missing formations. Reuse exact positive-only Momentum and Breakout baselines. Two new candidates only, with no parameter search:

- **Cumulative:** the trapezoidal time-weighted average of `log(P_t/P_first)` over the viewed interval, divided by its elapsed duration. Complete daily windows have N observations and N−1 elapsed days. Compute log ratios as differences of logs for numerical stability. Select scores strictly greater than zero.
- **Hybrid:** half the signed midrank percentile of signed endpoint log return plus half the signed midrank percentile of Cumulative. Rank positive magnitudes and negative magnitudes in separate pools; zero contributes zero, ties use `(below + 0.5*equal)/pool_size`, and negative magnitudes receive a negative percentile. Only the shared eligible universe defines these pools. Select scores strictly greater than zero.

Sort by descending score, then immutable identity; select at most ten names in ten fixed 10% capital slots, leaving unselected or failed-entry slots as cash. An earlier rally that faded below its starting price can still have positive Cumulative; it is therefore intentionally possible to select an endpoint loser. Do not force an extra positive-endpoint filter or retune weights.

Entry and exit remain actual executable opens at signal+2 and signal+32 days. Missing entries stay cash. Missing exits after an entry receive both zero-gross-with-round-trip-fees and total-loss marks, each at 50 and 100 bps per side. Future high/low paths are labels and excursion diagnostics only, never fills. Compute new identity/signal/H30 outcomes through the hash-pinned horizon grader; reuse and verify existing outcomes exactly where available.

Report paired daily returns against both Momentum and Breakout for every view, year, all years, 2020–2022 and 2023–2025. Report observed/realized +20% hits, known losses, missing paths, used and cash slots. Daily cohorts overlap and cannot be compounded. No trained chooser or new hold optimization occurs; the earlier/later split describes consistency of these fixed definitions.

Replay all four methods over 2023–2025 using the existing responsive batch scheduler for each of 32 start phases. Keep H30 deadlines, cross-year positions, original identity units and partial-fill cash slots. Compare wealth, annual checkpoint returns, drawdown, best-batch removal and paired phase differences under all four marks. Exact Momentum and Breakout baseline paths must reproduce the frozen daily capital ledger.

Also isolate selection versus position count: on each new candidate's signal, take the top K **signed endpoint Momentum** identities from the same eligible universe, K equal to its selected count. Unlike the earlier positive-only matched benchmark, this signed counterfactual permits endpoint losers when the candidate count exceeds the positive Momentum pool; report how often that occurs and label it explicitly. It is a benchmark on candidate timing/count, not a new standalone strategy. Reuse each candidate capital schedule unchanged. If benchmark fills would overlap the next original decision, mark that matched capital path unsupported; do not silently borrow capital, reschedule or remove a trade. Keep all daily comparisons regardless.

Before evaluation run synthetic checks for equal endpoints with different interim paths, flat prices, time-weighted integration, future-prefix invariance, signed pools/zeros/ties, immutable selection, entry delay and missing marks. Independently reconstruct a deterministic sample of raw cumulative scores and every saved selection's ordering, fixed baseline cohorts, baseline capital paths and all capital multiplication/annual/drawdown/best-batch arithmetic. Hash frozen helper code, source files, protocol, runners and ledgers; save UTC dates and refuse output overwrite. Do not edit shipped OHLC methods or reuse their forecast calibration for these new populations.
