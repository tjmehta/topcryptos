# Daily breakout paired sell policies — 2026-09-13

Written before daily exit results. This extends the already examined retrospective Binance panel; it is not an untouched holdout. Use the root daily study's exact frozen identities, signal dates, selected positions, ten-slot denominator and 30-day holding deadline. Compare every selected outcome, including Momentum, Breakout and VolumeBreakout for all twelve views. Early exit proceeds stay cash through the original deadline; these overlapping daily cohort averages are not compounded portfolio returns.

Import the frozen `research/sell-zones/2026-09-13/run.py` evaluator after checking its SHA-256 and its helper hash against that study's manifest. Policies and numerical thresholds remain unchanged: FixedH; SMAATRBracket (entry open +3 simple ATR14 target, max(0, entry open −2 ATR14) stop); ResistanceSMAATR (same bracket, target capped at the maximum high of the twenty complete days strictly before the signal, only if that resistance is above entry). ATR is the mean of fourteen consecutive true ranges through the signal, with a consecutive previous close; it is not Wilder ATR. Precompute indicators once per annual identity. No future bars can affect these levels.

Entry is the actual open signal+2 days. A daily close at/above target or at/below stop submits an exit filled at the actual open two calendar days later, capped by the original entry+30-day deadline. High/low crossings label potential target/stop reach only; they never fill an order. A missing level or missing complete bar before a confirmed trigger makes trigger ordering unknown; do not silently fall back to FixedH. A gap after a confirmed trigger does not undo the known trigger, but a missing execution open makes the exit unknown. Each retained outcome records all levels, trigger dates, executed prices, status and reason. Reach labels are true for an observed crossing, false only for a complete full-horizon path with no crossing, otherwise unknown.

Preserve the root missing-entry-as-cash treatment. An entered trade with unknown exit receives both zero-gross (round-trip fees still charged) and total-loss marks. Record 50 and 100 bps per side using identical decisions and actual entry/exit prices. All four FixedH marks must reproduce the root ledger exactly. Aggregate every method/view/year, all years and the pre-2023 versus 2023–2025 periods, retaining empty/cash cohorts. Report all policy means, paired differences versus FixedH, winning/losing signal dates, best-date removal, status/reason counts and realized holding durations. No threshold search, predictive fit or automatic production adoption occurs in this study.

Before writing results run the frozen synthetic tests plus local boundary tests for missing levels, missing interior history, delayed stop/target executions, hard deadline, gap after an established trigger, and both cost settings. Independently recompute every outcome's trigger chronology and four return marks without calling the imported evaluator; compare cached levels to the original prefix-only implementation on a deterministic sample, plus prefix/future-data and missing-history synthetic cases. Verify root input hashes, save input/source/protocol hashes, deterministic compressed ledger hash, and UTC start/completion dates. Refuse to overwrite existing results.
