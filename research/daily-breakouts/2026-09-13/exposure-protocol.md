# Exposure-matched Momentum comparison — 2026-09-13 UTC

The daily breakout and capital results have already been inspected. This is a post-result paired diagnostic of whether differences come from selecting different coins or simply taking fewer positions. It is retrospective, with no tuning or new strategy selection.

For every saved daily formation, compare each Breakout and VolumeBreakout selection against the first K saved Momentum selections on that exact signal/view, where K is the candidate's selection count. A positive breakout implies positive endpoint momentum; assert Momentum has at least K positions. Preserve candidate and benchmark order, ten equal capital slots and identical unused slots. Use existing identity/signal/H30 outcomes only, with no fresh scoring, price recovery or universe changes. Missing entries remain cash; missing exits retain the same zero-gross and total-loss marks, with 50bps and100bps costs per side.

Report every view and method for all years, each year and both2020–2022/2023–2025 halves. Compare mean four-scenario cohort returns, paired mean/median/wins/losses, observed+20% daily-high reach hits, realized+20% net-return hits and known losing trades. Report missing entries/exits, unknown paths and selected counts so these diagnostics cannot mistake unknown outcomes for confirmed failed alerts.

Also replay each existing candidate capital path (both schedulers, all32 initial dates), replacing only selected coins with the matched top-K Momentum identities. Preserve every original candidate decision date and next-available date, even if benchmark entry availability differs; this is a paired counterfactual on the candidate schedule, not a standalone Momentum trading policy. Keep returns until each original fixed hard deadline; no early proceeds are reinvested. A benchmark fill must not remain open past the next reserved signal; explicitly check and report any conflict rather than silently violating the capital constraint. Empty candidate decisions imply an empty benchmark. Missing formations remain missing. Report paired terminal wealth, annual deadline-checkpoint returns, drawdown, filled counts, and every scenario for each path; aggregate differences across phases without selecting a favorable phase.

Save complete source outcome-key lists and frozen schedules, source hashes and exact arithmetic checks in immutable outputs. Reproduce candidate cohort means and primary candidate terminal wealth before interpreting matched comparisons. The source outcomes and overlapping dates remain dependent; this comparison cannot establish a fresh statistical holdout or causal market superiority.
